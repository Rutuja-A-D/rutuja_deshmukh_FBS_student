//Write a program to find greatest of three numbers using nested if-else.
void main()
{
	int a=-12,b=-2,c=10;
	if(a>b)
	{
	 if(a>c)
	 printf("a is greter");
	 else
	printf ("c is greater");
	 	}
	 else if (b>c)
	 {
	 printf("b is greater");
      }
	 else
	{
	  printf("c is greater");
	 	 }
}